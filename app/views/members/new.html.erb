<%= form_for Member.new, :html => {:multipart => true} do |f| %>
    <p>
      <%= f.file_field :image %>
      <%= f.text_field :name %>
      <%= f.text_area :description %>
    </p>
    <p><%= f.submit %></p>
<% end %>